import("//build/shell_build.gni")

shell_build("armadillo") {
  script = "build_armadillo.sh"
  outputs = [ "${target_out_dir}/libarmadillo_engine2.a" ]

  deps = [ "//third_party/openblas:openblas" ]
}
